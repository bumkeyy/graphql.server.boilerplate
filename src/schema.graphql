type Query {
  getMachine(id: ID!): Machine
  getMachines: [Machine]
}

type MachinePublish {
  MachinePublish: [Machine]
}

type Subscription {
  MachineSubscription: String
}

type Machine {
  id: ID!
  name: String!
  type: String!
  state: String!
}
