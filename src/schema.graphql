type Query {
  getMachine(id: ID!): Machine
  getMachines: [Machine]
}

type Subscription {
  MachineSubscription: [Machine]
}

type Machine {
  id: ID!
  name: String!
  type: String!
  state: String!
}
